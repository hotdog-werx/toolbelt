profiles: {} # None - only overriding variables

global_exclude_patterns:
  - tests/integration/fixtures/**/*.py

variables:
  TB_RUFF_VERSION: latest
  TB_COVERAGE_TARGET: tests
  TB_PROJECT_SOURCE: toolbelt
